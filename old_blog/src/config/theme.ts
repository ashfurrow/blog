const colors = {
  primary: '#398CCC', // Color for buttons or links
  bg: '#fff', // Background color
  white: '#fff',
  grey: {
    dark: 'rgba(0, 0, 0, 0.9)',
    default: 'rgba(64, 64, 64)',
    light: '#eee',
    extraLight: 'rgba(0, 0, 0, 0.1)'
  }
}

const transitions = {
  normal: 'none'
}

const fontSize = {
  small: '0.9rem',
  big: '2.9rem'
}

export const theme = {
  colors,
  transitions,
  fontSize
}
