---
title: Blog  
---

{# TODO: Migrate this to a filter instead so I don't need to deal with JSON serialization. #}
{% set groupedPostsJson %}{% groupedPosts %}{% endset %}
{% set groupedPosts = groupedPostsJson | fromJson  %}

{%- for group in groupedPosts %}
  <div class="group">
    <h2>{{ group.name }}</h2>
    {%- for post in group.posts %}
      <article>
        <a href="{{ post.url }}">{{ post.title }}</a>
        <time datetime="{{ post.date | parseDate | htmlDateString }}">{{ post.date | parseDate | readableDate("DDD") }}</time>
      </li>
    {%- endfor %}
  </div>
{%- endfor %}

<style>
  .group {
    margin: 0;
    margin-bottom: 2rem;
  }
  .group h2 {
    margin-bottom: 0.5rem; 
    font-size: 1.5rem;
  }
  .group a {
    margin-right: 0.5rem;
  }
  time {
    color: rgba(0, 0, 0, 0.5);
  }
</style>
  